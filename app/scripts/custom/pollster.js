Yeomanwebapp.Pollster = {	
  // start: function(param){
  // 	debugger;
  // 	var param = param;
  //   this.timer = setInterval(this.onPoll(param).bind(this), 5000);
  // },
  // stop: function(){
  //   clearInterval(this.timer);
  // },
  onPoll: function(param){
  	debugger;
    param.refreshInvites();
  }
}